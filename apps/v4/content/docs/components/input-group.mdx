---
title: Input Group
description: 为输入框或文本域添加额外信息或操作的组合组件。
component: true
---

import { IconInfoCircle } from "@tabler/icons-react"

<ComponentPreview name="input-group-demo" className="[&_.preview]:p-4" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input-group
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>将下面的代码复制到你的项目中。</Step>

<ComponentSource name="input-group" title="components/ui/input-group.tsx" />

<Step>根据项目结构调整导入路径。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import {
  InputGroup,
  InputGroupAddon,
  InputGroupButton,
  InputGroupInput,
  InputGroupText,
  InputGroupTextarea,
} from "@/components/ui/input-group"
```

```tsx showLineNumbers
<InputGroup>
  <InputGroupInput placeholder="搜索..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
  <InputGroupAddon align="inline-end">
    <InputGroupButton>搜索</InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```

## 示例

### Icon

<ComponentPreview name="input-group-icon" className="[&_.preview]:p-4" />

### Text

在输入框旁展示额外文本信息。

<ComponentPreview name="input-group-text" className="[&_.preview]:p-4" />

### Button

在输入组中加入按钮以触发操作。

<ComponentPreview name="input-group-button" className="[&_.preview]:p-4" />

### Tooltip

添加提示信息以提供额外说明。

<ComponentPreview name="input-group-tooltip" className="[&_.preview]:p-4" />

### Textarea

输入组同样适用于文本域，可通过 `block-start` 或 `block-end` 控制对齐。

<ComponentPreview name="input-group-textarea" className="[&_.preview]:p-4" />

### Spinner

在处理输入时展示加载状态。

<ComponentPreview name="input-group-spinner" className="[&_.preview]:p-4" />

### Label

在输入组中添加标签提升可访问性。

<ComponentPreview name="input-group-label" className="[&_.preview]:p-4" />

### Dropdown

与下拉菜单组合实现更复杂的交互。

<ComponentPreview name="input-group-dropdown" className="[&_.preview]:p-4" />

### Button Group

结合按钮组创建前缀或后缀。

<ComponentPreview
  name="input-group-button-group"
  className="[&_.preview]:p-4"
/>

### Custom Input

为自定义输入添加 `data-slot="input-group-control"` 属性，即可自动获得行为与聚焦状态处理。

组件不会对自定义输入应用样式，可通过 `className` 自行定制。

<ComponentPreview
  name="input-group-custom"
  className="!mb-4 [&_.preview]:p-4"
/>

```tsx showLineNumbers
import { InputGroup, InputGroupAddon } from "@/component/ui/input-group"
import TextareaAutosize from "react-textarea-autosize"

export function InputGroupCustom() {
  return (
    <InputGroup>
      <TextareaAutosize
        data-slot="input-group-control"
        className="dark:bg-input/30 flex field-sizing-content min-h-16 w-full resize-none rounded-md bg-transparent px-3 py-2 text-base transition-[color,box-shadow] outline-none"
        placeholder="Autoresize textarea..."
      />
      <InputGroupAddon align="block-end">how</InputGroupAddon>
    </InputGroup>
  )
}
```

## API 参考

### InputGroup

负责包裹输入与附加内容的主组件。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<InputGroup>
  <InputGroupInput />
  <InputGroupAddon />
</InputGroup>
```

### InputGroupAddon

用于在输入两侧展示图标、文本、按钮等内容。

<Callout icon={<IconInfoCircle />} title="Focus Navigation">
  为确保焦点顺序正确，应将 `InputGroupAddon` 放在输入控件之后，并通过 `align` 属性调整位置。
</Callout>

| Prop        | Type                                                             | Default          |
| ----------- | ---------------------------------------------------------------- | ---------------- |
| `align`     | `"inline-start" \| "inline-end" \| "block-start" \| "block-end"` | `"inline-start"` |
| `className` | `string`                                                         |                  |

```tsx
<InputGroupAddon align="inline-end">
  <SearchIcon />
</InputGroupAddon>
```

**在 `<InputGroupInput />` 中使用 `inline-start` 或 `inline-end`，在 `<InputGroupTextarea />` 中使用 `block-start` 或 `block-end`。**

`InputGroupAddon` 内可以包含多个 `InputGroupButton` 以及图标。

```tsx
<InputGroupAddon>
  <InputGroupButton>Button</InputGroupButton>
  <InputGroupButton>Button</InputGroupButton>
</InputGroupAddon>
```

### InputGroupButton

在输入组中展示按钮。

| Prop        | Type                                                                          | Default   |
| ----------- | ----------------------------------------------------------------------------- | --------- |
| `size`      | `"xs" \| "icon-xs" \| "sm" \| "icon-sm"`                                      | `"xs"`    |
| `variant`   | `"default" \| "destructive" \| "outline" \| "secondary" \| "ghost" \| "link"` | `"ghost"` |
| `className` | `string`                                                                      |           |

```tsx
<InputGroupButton>Button</InputGroupButton>
<InputGroupButton size="icon-xs" aria-label="Copy">
  <CopyIcon />
</InputGroupButton>
```

### InputGroupInput

在输入组场景下替代 `<Input />`，预置了输入组样式，并通过统一的 `data-slot="input-group-control"` 处理焦点状态。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

其余属性会透传给底层 `<Input />` 组件。

```tsx
<InputGroup>
  <InputGroupInput placeholder="Enter text..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

### InputGroupTextarea

输入组场景下替代 `<Textarea />`，预置文本域样式，并使用统一的 `data-slot="input-group-control"` 处理焦点状态。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

其余属性会透传给底层 `<Textarea />` 组件。

```tsx
<InputGroup>
  <InputGroupTextarea placeholder="Enter message..." />
  <InputGroupAddon align="block-end">
    <InputGroupButton>Send</InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```

## 更新日志

### 2025-10-06 `InputGroup`

为 `InputGroup` 组件新增 `min-w-0` 类。详见 [diff](https://github.com/shadcn-ui/ui/pull/8341/files#diff-0e2ee95d0050ca4c5d82339df86c54e14a6739dc4638fdda0eec8f73aebc2da9)。
